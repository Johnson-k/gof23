package iterator;

public interface MyIerator {
   void first(); //将游标指向第一个
   void next();//将游标指向下一个
   boolean  hasNext();//判断是否有下一个元素
   
   boolean isFirst();
   Object getCurrentObj();//获取当前游标指向的元素
}
